# 2026-08-09 reviewed

## プロンプト
あなたは、Copilotパイセン。

われわれのプロジェクトをシニアエンジニアとして見守る立場のパイセンです。

現在の`.dev_helper`プロジェクトについて、下記の観点からレビューしてください。

- `Dev_`で始まるモジュールの依存関係
- 現在の実装の問題点"

---

## 依存関係

`.dev_helper` にある `Dev_` 系モジュールの依存関係は、かなり単純です。

- DevUtils.bas
  - 基盤のユーティリティモジュール
  - `VBIDE` 定数相当の `Enum`
  - コード読み込み／比較／抽出／コンポーネント検索／エラー生成などを提供
  - 他の `Dev*` モジュールから参照される一方向依存

- DevProjIO.bas
  - `DevUtils` に依存
  - サブフォルダ構造 `repo\std_mod\`, `repo\cls_mod\`, `repo\frm_mod\`, `repo\doc_mod\` を使って
    - `ExportComponentsToRepository`
    - `ImportComponentsFromRepository`
    - `ImportComponent`
    - `ExportComponent`
  - 通常プロジェクトのモジュールの import/export を扱う

- DevBootstrap.bas
  - `DevUtils` に依存
  - `.dev_helper` フォルダへ `Dev*` モジュールだけを push し、
  - `Dev*` モジュールを `.dev_helper` から pull する
  - `DevProjIO` の処理を再利用せず、独自の push/pull ロジックを持つ

- ForPullTest.bas
  - テスト・サンプル用途
  - `DevUtils.ExecuteAllExperiments` で参照されるが、実運用の依存グラフには含まれない

依存の全体像は、次のようになります。

- DevUtils.bas : 基底
- DevProjIO.bas → DevUtils.bas
- DevBootstrap.bas → DevUtils.bas

`DevProjIO` と `DevBootstrap` の間には直接依存はありません。

---

## 現在の実装の問題点

### 1. 2つの別々の同期ワークフローが混在している
- DevBootstrap.bas は `.dev_helper` フォルダに `Dev*` モジュールを出し入れする専用フロー
- DevProjIO.bas は `repo` フォルダ配下の `std_mod/`, `cls_mod/` などを使う別フロー
- これにより、機能が重複しやすく、運用ルールが分かれています

### 2. `DevBootstrap.PullDevModules` にディレクトリ存在チェックがない
- `PullDevModules` は `m_fso.GetFolder(repodir)` をそのまま使う
- `.dev_helper` フォルダが存在しない場合、例外が発生する可能性があります

### 3. 行区切り処理が脆い
- `DevUtils.HasCustomAttribute` / `ExtractCodeBody` / `FindCodeStartLine` が
  - `Split(conts, vbCrLf)` に依存
- もしファイルが `LF` だけ、あるいは `CR` だけの行区切りだった場合に期待どおりに動かない恐れがあります
- `LoadComponentCode` での読み込み後に正規化していないため、堅牢性が低い

### 4. カスタム属性対応の制限
- `DevProjIO.ImportComponent` は
  - リポジトリ側にカスタム属性がある場合、`comp` を丸ごと再インポート
  - ドキュメントモジュールでカスタム属性があると「自動同期不可」でスキップ
- これは VBA の制約上仕方ない面はあるものの、同期できないケースが存在する点は運用上の弱点です

### 5. `DevBootstrap` と `DevProjIO` の責務切り分けが曖昧
- どちらも「VBProject とファイルの同期」をするが、対象とフォルダ構造が違う
- `DevUtils` の関数を共通化しているものの、上位レイヤーの設計が分離されているので
  - 同じ振る舞いを修正したいときに二箇所直す必要が出る

### 6. エラーメッセージ／例外処理の一貫性
- `DevUtils.RaiseError` は全例外に `"(ﾟдﾟ)､ < クソが。"` を付加
- 開発者向けとしては個性あるが、ライブラリとしてはやや雑で再利用性が下がる

### 7. `ExtractModuleName` のパースはやや脆弱
- `Attribute VB_Name` の解析を手作業で行っているため、フォーマットが微妙に違うと失敗しやすい
- ここは VBA モジュールファイルのフォーマットに依存した実装

---

## まとめ

### 良い点
- `DevUtils` で共通処理をまとめていて、依存は一方向
- `DevProjIO` と `DevBootstrap` の責務はそれぞれ明確に分かれている

### 気になる点
- 同じ `.dev_helper` プロジェクト内で、同期方式が 2種類ある
- `PullDevModules` の存在チェック不足
- 行区切り正規化処理の弱さ
- カスタム属性対応の限界とドキュメントモジュールの同期不能ケース

もし要望があれば、次は
1. `DevBootstrap` と `DevProjIO` の同期ロジック統合案、
2. `ExtractCodeBody` の行末正規化改善、
3. `PullDevModules` の堅牢化、
のいずれかについて詳しく提案します。

# 2026-08-08 reviewed

## プロンプト

`.dev_helper`プロジェクト全体について、シニアエンジニアの視点でレビューしてください。

単なる個別のコードレビューではなく、プロジェクト全体の設計・責務分担・依存関係・可視性（Public / Private）・エラーハンドリング・保守性・VBA/VBIDE固有の制約への対応などを俯瞰して評価してください。

また、現在の実装と`copilot-instructions.md`とを比較し、以下についても検討してください。

- `copilot-instructions.md`の内容が現在の実装と乖離していないか
- 現在の実装に対して、`copilot-instructions.md`に不足している設計上・実装上のルールがないか
- `copilot-instructions.md`に記載されている方針のうち、現在の実装に照らして見直すべきものがないか

レビューでは、問題点を重要度や優先度が分かるように整理してください。

また、現状の設計を尊重したうえで、本当に修正する価値がある問題と、単なる好み・一般論に留まる提案は区別してください。

今回はレビューのみとし、コードの変更は行わないでください。

## 総評

全体として、このプロジェクトは「VBAをGitベースの開発フローに乗せるための実用ライブラリ」として、かなりまとまった設計になっています。特に、DevBootstrap.bas と DevProjIO.bas の役割分担は、現状の目的に対してかなり妥当です。

特に良い点は、以下です。

- 目的が明確で、ライブラリ本体と対象プロジェクトのソース群を分けて管理する構成が自然です。
- VBA/VBIDEの制約に合わせて、カスタムAttributeやDocument moduleを別扱いする方針が入っています。
- bulk import/exportで個別失敗を握りつぶしつつも、処理を続行する設計は、VBA環境では実用的です。
- 依存方向も、指示書の意図に沿って比較的きれいに保たれています。

ただし、今後の保守性を考えると、いまの実装は「動く」段階に達している一方で、設計の境界が少し曖昧になってきているため、ここを詰めないと長期的な負債が増えやすいです。

---

## 優先度付きの主要な指摘

### 優先度A：修正の価値が高い

1. Public/Privateの境界が曖昧で、内部実装の公開API化が進んでいる
- \.dev_helper/DevBootstrap.bas には、実装補助用の関数群がかなり多く、公開範囲が広いです。
- たとえば、コード抽出・比較・モジュール名抽出・拡張子判定などは、ライブラリ利用者向けではなく「DevHelper自身のメンテナンス補助」に近い処理です。
- これがPublicだと、将来のAPIとして扱われやすく、内部実装の変更が外部依存に波及しやすくなります。
- これは単なるスタイル問題ではなく、保守性と契約の明確さに直結する重要な問題です。

2. モジュールレベルの状態が暗黙依存として多く、責務が分散している
- \.dev_helper/DevProjIO.bas では、`m_fso`、`m_SucceededCount`、`m_FailedCount`、`m_SkippedCount`、`m_Warnings`、`m_OnSharePoint` などがグローバルに近い状態を保持しています。
- これはVBAではよくある実装ですが、処理の流れを追うときに「どこで何が設定されているか」を意識しないと理解しにくくなります。
- 特に、`ImportComponent` や `ExportComponent` のような中核手続きは、状態・エラー処理・差分判定・置換まで多くを担当しており、責務が重いです。

3. 機密情報がそのままコードに混入している
- Std01Main.bas に、接続文字列とユーザー名・パスワードのような情報が埋め込まれています。
- これはサンプルコードとして存在するならまだ説明可能ですが、共有・再利用・再配布を前提にするなら、かなり危険です。
- これは設計だけでなく、運用・セキュリティ面でも重要です。

### 優先度B：次点で直す価値がある

4. エラー処理が「続ける」方向には寄っているが、可観測性が弱い
- bulk import/exportでは、個別失敗を握りつぶして処理を継続する方針は妥当です。
- ただし、現在の実装は主にImmediate Windowへの出力に依存しており、利用者が「何が失敗し、何がスキップされたか」を把握しづらいです。
- ここは「例外を投げる」でも「全部握りつぶす」でもなく、失敗理由を集約して返す/表示する形に寄せた方が、将来の保守性が上がります。

5. コア同期処理の回帰テストが弱い
- import/exportの主要ロジックは、VBA開発においてかなり重要な箇所ですが、現状は手動確認用の実験用手続きが中心です。
- これはVBAの実行環境上では仕方ない面もありますが、少なくとも「カスタムAttribute」「Document module」「既存ファイル差分あり/なし」のようなケースは、テスト可能な形でカバーしたいところです。
- 重要度としては高いです。特に、このライブラリの本体が壊れたときの被害が大きいので。

### 優先度C：将来の整理として価値があるが、今すぐではない

6. 1つの手続きが多くの責務を抱えている
- \.dev_helper/DevProjIO.bas の `ImportComponent` や `ExportComponent` は、かなり多機能です。
- これは現状でも動作はしますが、将来的に拡張するたびに理解コストが上がります。
- ここは「大規模なリファクタリング」ではなく、責務ごとに小さなヘルパーへ分解する程度で十分です。

---

## copilot-instructions.mdとの比較

### 現在の実装と概ね整合している点
- 依存方向の考え方は概ね守られています。
- VBIDEの制約に合わせた設計、特にカスタムAttributeやDocument moduleの扱いは、指示書の意図に合っています。
- bulk import/exportで個別失敗を許容する方針も、指示書の方針に合っています。
- `Dev*`モジュールのメンテナンス用途という考え方も、実装の雰囲気としては自然です。

### 乖離している点
- 指示書では「Public APIは最小限に」「メンテナンス専用の手続きはPrivate寄りに」という方針ですが、実装ではその境界がかなり曖昧です。
- 指示書では「RaiseError()を使う」とありますが、実装上は多くのケースで `Debug.Print` と握りつぶしに寄っており、実運用時の可観測性とのバランスが少し弱いです。

### 指示書に追加するとよいルール
以下は、現状の実装を踏まえると、指示書に入れておく価値があります。

- 「内部補助用関数は、明示的に公開APIとして扱う場合を除き、Publicにしない」
- 「コア同期処理は、モジュールレベルの可変状態に依存しすぎない。必要なら引数やコンテキストオブジェクトで渡す」
- 「接続文字列・認証情報・環境依存値は、コードに直接埋め込まない」
- 「import/exportの核となる処理には、少なくとも主要ケースの回帰テストを用意する」
- 「スキップや失敗が発生した場合は、単なるDebug.Printではなく、集約された要約を返す/表示する」

---

## 「本当に価値がある修正」と「好み・一般論」

### 価値がある修正
- Public/Private境界の整理
- モジュールレベルの状態の見直し
- エラー情報の集約と可観測性向上
- 機密情報の外部化
- コア同期ロジックのテスト化

### 単なる好み・一般論に留まるもの
- `Call`キーワードの有無
- `getSqlFilePath` のような命名の微調整
- コメントの日本語/英語の混在
- 小さな手続きの再配置

---

## 結論

このプロジェクトは、方向性としてはかなり良いです。特に、VBA/VBIDEの制約を踏まえた設計は妥当で、今のままでも十分実用的です。

ただし、今後の保守性を重視するなら、最優先で直したいのは次の3点です。

1. Public/Privateの境界を明確にする
2. モジュール状態と暗黙依存を減らす
3. エラーと失敗情報を、利用者にとって追いやすい形で扱う

これらは大規模な再設計ではなく、現状の設計思想を尊重したまま実現できる改善です。

# 2026-08-01 reviewed

## レビュー結果 その3

---

___`copilot-instructions.md`を再修正し、みたびレビューを依頼。___

---

追記内容を踏まえて、設計意図と実装の整合性が取れているかを中心に再確認します。

今回の設計意図を踏まえると、プロジェクト全体としてかなり一貫性があります。特に、DevBootstrap.bas と DevProjIO.bas の構成は、今の方針にかなりよく合っています。

### 良い点
- DevBootstrap.bas では、`Dev` 系モジュールのメンテ用途処理と共通ユーティリティが自然に分かれており、`Public` 手続きが利用者向け API とは限らないという前提に沿っています。
- DevProjIO.bas は、公開的な入口である `ExportComponentsToRepository` を中心に、内部ヘルパと実験用コードが分かれていて、責務の分離は比較的明確です。
- `AA_Experiments` 系の手動検証コードが意図的に残されている点も、今回の説明に照らすと自然です。VBA の制約上、同一モジュールに置く構成も妥当です。

### 気になる点
- ただし、今の実装だと「どの `Public` 手続きが利用者向けで、どれがメンテ用途か」をコード上で読む人が見抜きにくい箇所があります。
- 特に DevBootstrap.bas では、今後の保守者が「これは API なのか、単なる開発支援なのか」を見失いやすい可能性があります。
- 実験用コードそのものは問題ありませんが、将来的にはコメントや区分けをもう少し強めると、意図がより伝わりやすくなります。

### 総評
全体として、今回追記された設計意図と実装の方向性はかなり整っています。大きな構造的なズレは見当たらず、むしろこのプロジェクトは「意図を明文化した上で保守しやすい形」に近づいている印象です。

> 重要なのは、構造を大きく変えることではなく、今後の読者が意図をすぐ理解できるようにすることです。そこだけ少し補強すると、より安定した設計になります。

## レビュー結果 その2

---

___`copilot-instructions.md`を修正し、再レビューを依頼した。___

---

前回の観点を踏まえつつ、今回の差分と構造を再確認してレビューを整理します。

再度見渡した限り、全体としてかなり良い状態です。特に、DevBootstrap.bas と DevProjIO.bas の責務が比較的明確で、VBA らしい小さな構成になっています。

### 良い点
- 役割が分かれています
  - DevBootstrap.bas は共通定義・ヘルパ・DevHelper 自体のメンテ用処理
  - DevProjIO.bas はリポジトリへのエクスポート処理
- 命名とコメントが読みやすく、保守しやすいです。
- 独自エラー処理の方針があり、VBA でも比較的整理されています。
- 現在のワークスペース診断では、DevBootstrap.bas と DevProjIO.bas のエラーは確認されませんでした。

### 気になる点
- 依存関係と「公開 API」と「メンテ用入口」の境界が少し曖昧です。
  - 例えば、DevBootstrap.bas の `PushDevModules` / `PullDevModules` は内部実装寄りですが、用途が広く見えるため、今後の保守で迷いやすいです。
- エクスポート失敗時の扱いが弱いです。
  - DevProjIO.bas では失敗が `Debug.Print` に寄っており、実運用では追跡しにくいです。
- パスルールが分散しています。
  - `.dev_helper` 用パスと `repo` 用パスの管理が別々で、拡張時にずれやすいです。
- 実験用手続きが残っています。
  - DevProjIO.bas の `Exp_...` 系は参考にはなりますが、ライブラリとしては整理した方が自然です。

### 優先度の高い改善案
1. メンテ用途の入口と利用者向け API をより明確に分ける
2. エクスポート失敗時の通知方法を強化する
3. パス定義を一元化する
4. 実験用コードを分離または削除する

## 総評
このプロジェクトは「小さくて理解しやすい」「今後の拡張にも耐えやすい」方向に進んでいます。現状でも十分に読みやすく、次の一歩としては「運用耐性」を少しだけ上げると、かなり良いライブラリになります。

## レビュー結果 その1

全体として、かなり整理された小さな VBA ライブラリです。責務が比較的明確で、DevBootstrap.bas では共通定義・ヘルパ、DevProjIO.bas ではリポジトリ出力周りを分けており、初学者が追いやすい構成です。

### 良い点
- 役割分担が自然です。
  - DevBootstrap.bas は定数・共通ユーティリティ・モジュール同期の土台。
  - DevProjIO.bas は実際のエクスポート処理に集中。
- 命名とコメントが比較的読みやすく、VBA らしい書き方に寄っています。
- 独自エラー定義と `RaiseError()` を使っており、例外の扱い方が統一されています。
- 現時点の VS Code 診断では構文エラーは見つかっていません。

### 気になる点
- いくつかの処理が「内部実装」と「公開 API」の境界が曖昧です。
  - たとえば DevBootstrap.bas の `PushDevModules` / `PullDevModules` は private ですが、用途が見えにくく、将来の保守で迷いが出そうです。
- エクスポート失敗時の扱いがやや弱いです。
  - DevProjIO.bas は失敗を `Debug.Print` で握りつぶしていますが、実運用では「どのファイルが失敗したか」を集約して返すか、少なくとも明示的に報告する方が安全です。
- パス管理が分散しています。
  - `.dev_helper` 用ディレクトリと `repo/` 用ディレクトリのルールが別々にあり、今後の変更でズレやすいです。
- 破壊的な操作が含まれます。
  - 既存ファイルを削除してからエクスポートする挙動は、誤実行時のリスクがあります。確認フローやバックアップ方針があるとより安心です。
- 実験用サブルーチンが残っています。
  - DevProjIO.bas の `Exp_...` 系は実装の参考にはなりますが、公開前の整理としては削除・分離した方がきれいです。

### 優先度の高い改善案
1. 主要操作の公開 API を明確にする
2. エクスポート失敗時の報告方法を強化する
3. パス定義を一か所にまとめる
4. 実験用コードを分離または削除する

## 総評
「小さくて分かりやすい、でも実運用に向けては少しだけ堅牢性を上げたい」タイプの実装です。現状のままでも理解しやすく、今後の拡張性も十分あります。